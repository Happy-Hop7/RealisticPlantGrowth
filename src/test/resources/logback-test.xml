<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- Add %logger{0} to include the logger name, similar to Minecraft -->
            <pattern>[%d{HH:mm:ss} %level]: [%logger{0}] %msg%n</pattern>
        </encoder>
    </appender>
    <root level="info">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
